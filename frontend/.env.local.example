# Base URL of the FastAPI backend (include the /api suffix). Per CLAUDE.md §1.
# Local:
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api
# Production (Vercel): set to your Render backend URL, e.g.
# NEXT_PUBLIC_API_BASE_URL=https://airbnb-clone-api.onrender.com/api
